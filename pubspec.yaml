name: futurebuilder_example
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  cupertino_icons: ^1.0.0
  shared_preferences : ^2.0.6
  http: ^0.12.2
  intl : ^0.17.0
  syncfusion_flutter_charts: ^19.2.46+1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/users.json